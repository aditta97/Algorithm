#include <iostream.h>
#include <cstdio.h>
int main(){
        int i, j, a[5], b[5];

        for(i = 0; i <= a[i]; i++) {
                for(j = 0; j <= b[i]; j++) {
                        printf("%d %d", a[i], b[j]);
                }
        }
}
